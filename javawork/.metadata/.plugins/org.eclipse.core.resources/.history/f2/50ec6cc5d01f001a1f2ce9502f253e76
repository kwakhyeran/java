package workbook77p;

import workbook74p.Rectangle;
import workbook74p.Triangle;

public class CarTest {

	public static void main(String[] args) {
		
		Car[] ca = new Car[2];
	
		ca[0] = new L3("L3","1500" ,50,25,0);
		ca[1] = new L5("L5","2000" ,70,35,0);
	}
	
	System.out.println("vehicleName"+"engineSize"+"oilTack"+"oilSize"+"distace"+"temperature");
	
	for (int i = 0; i < ca.length; i++) {
		if (ca[i] instanceof Rectangle) {
			
			
			
			
		} else if (shape[i] instanceof Triangle) {
			name = "Triangle";
		} else {
			name = "Circle";

		}
		System.out.println(name + "\t" + shape[i].getArea() + "\t" + shape[i].getColors() );
	}
	

}
